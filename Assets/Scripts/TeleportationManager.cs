using System.Collections;
using System.Collections.Generic;
using UnityEngine;
// using UnityEngine.InputSystem;

public class TeleportationManager : MonoBehaviour
{

    /*[SerializeField] private InputActionAsset actionAsset;
    void Start()
    {
        var activate = actionAsset.FindActionMap("XRI Lefthand").FindAction("Teleport");
        activate.Enable();

        var cancel = actionAsset.FindActionMap("XRI Lefthand").FindAction("Teleport Move");
        cancel.Enable();

        // _thumbstick = actionAsset.FindActionMap("XRI Lefthand").FindAction("Move");
        // _thumbstick.Enable();
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
